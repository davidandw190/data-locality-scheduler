# # Create a file named scheduler-prometheus-rules.yaml
# apiVersion: monitoring.coreos.com/v1
# kind: PrometheusRule
# metadata:
#   name: scheduler-comparison-rules
#   namespace: monitoring
# spec:
#   groups:
#   - name: scheduler-comparison
#     interval: 30s
#     rules:
#     - record: scheduler:pod_locality_ratio
#       expr: |
#         sum by (scheduler_name) (rate(scheduler_pod_placement_total{node_type="edge"}[5m])) / 
#         sum by (scheduler_name) (rate(scheduler_pod_placement_total[5m]))

#     - record: scheduler:data_transfer_efficiency
#       expr: |
#         sum by (scheduler_type) (rate(eo_data_transfer_bytes_total{destination="local"}[5m])) / 
#         sum by (scheduler_type) (rate(eo_data_transfer_bytes_total[5m]))

#     - record: scheduler:workflow_completion_time
#       expr: |
#         sum by (scheduler_type, workflow) (rate(eo_processing_time_seconds_sum[10m])) / 
#         sum by (scheduler_type, workflow) (rate(eo_processing_time_seconds_count[10m]))
